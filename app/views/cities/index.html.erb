<h1>Cities To Choose</h1>

<ul id="cities">
 <% @cities.each do |city| %>
   <li>
    <%= link_to city.name, city_path(city) %>
   </li>
  <% end %>
 </ul>

 <%= link_to "Add a city", new_city_path, class: "new_city" %> 