<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
	<% @user.each do |user| %>
		<li>
			<h2>
			  <%= gravatar_for user, size: 40 %>
			  <%= user.username %> - 
			  <%= user.email %>
			</h2>
		</li>
	<% end %>
</ul>