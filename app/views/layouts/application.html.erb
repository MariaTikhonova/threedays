<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield :title) %></title>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <%= render 'layouts/rails_default' %>
  </head>
  <body>
    <%= render 'layouts/header' %>


  	<div class="container">
    
      <div class="alert-success" role="alert"><%= notice %></div>
      <div class="alert-danger" role="alert"><%= alert %></div>
    	
      <%= yield %>
    
    <%= debug(params) if Rails.env.development? %>
    </div>

    <%= render 'layouts/footer' %>
  </body>
</html>
